# DataVine-Analytics
Prototype ML suite for classification (k-NN), recommendation (cosine similarity), and clustering (K-Means/GMM) with PCA and hyperparameter tuning — built for DataVine Analytics' retail, agriculture, and public-policy clients.
